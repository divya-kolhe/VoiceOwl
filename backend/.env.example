# Mongo: set either MONGO_URI to an existing Mongo Atlas / local MongoDB.
MONGO_URI=mongodb://localhost:27017/

# If you want to use in-memory Mongo for tests/dev, leave MONGO_URI empty.

PORT=4000

# Azure (optional; service will fallback to stub if missing)
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=
